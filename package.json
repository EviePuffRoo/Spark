{
  "name": "spark",
  "private": true,
  "version": "0.1.0",
  "description": "Prep-and-run companion for tabletop RPGs, from content generation to a live shared Battle Grid",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "start:server": "npm run start --workspace server",
    "build": "npm run build --workspace client"
  }
}
